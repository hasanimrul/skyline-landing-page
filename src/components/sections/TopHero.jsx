import React from 'react';

const TopHero = () => {
    return (
        <div style={{backgroundImage: "url('/assets/images/hero-bg.png')", backgroundSize: "contain", backgroundPosition: "right", backgroundRepeat: "no-repeat"}} className="  h-screen flex items-center">
           
        </div>
    );
};

export default TopHero;